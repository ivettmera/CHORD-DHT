# Network configuration for Chord DHT deployment
# Edit these IPs to match your actual VM external IPs

# VM1 (Bootstrap) - us-central1
VM1_IP=34.38.96.126

# VM2 - europe-west1  
VM2_IP=35.199.69.216

# VM3 - asia-east1
VM3_IP=34.58.253.117

# Bootstrap address (typically VM1)
BOOTSTRAP_ADDR=34.38.96.126:8000

# Port assignments per VM
VM1_PORT=8000  # Bootstrap node
VM2_PORT=8001  # Second node
VM3_PORT=8002  # Third node

# Default ports for multiple nodes per VM (if needed)
BASE_PORT=8000
NODE_COUNT_PER_VM=3

# Deployment settings
LOG_LEVEL=info
METRICS_INTERVAL=5s